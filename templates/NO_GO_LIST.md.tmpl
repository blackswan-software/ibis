# No-Go List — {{PROJECT_NAME}}

_Things EXPLICITLY excluded from the plan. Read before proposing strategy._

_Adding to this list is a deliberate decision — not a default. Each entry
records WHAT is excluded and WHY, so future sessions don't re-propose ideas
that were already considered and rejected._

## Template for new entries

```markdown
- **NO [thing].** [Why it's excluded — the reasoning, not just the rule.]
```

## Excluded decisions

<!-- Example entries:

- **NO microservices migration.** Current monolith handles load; the team
  is 3 people and can't operate 12 services. Revisit at 10 engineers.

- **NO Kubernetes.** Docker Compose on 2 VMs covers current scale. K8s
  operational overhead exceeds the benefit. Revisit at 50K daily requests.

- **NO free tier.** Pricing model requires paid-from-day-1 to filter for
  serious users. Free trials attracted support load with no conversion.
-->

## Memory rule

If you find yourself recommending anything on this list, STOP. Re-read the
entry. The recommendation is wrong even if your training data suggests it's
standard advice. The entries here represent considered decisions, not
oversights.
