# {{NODE_NAME}}

> ibis node doc — **required**. `ibis doctor` fails if this file is missing.
> Replace the TODOs. This is what the next person (or agent) reads when the
> node's health check fails.

## What it is

{{NODE_NAME}} — TODO: one or two sentences. What does this component do, and
what breaks downstream if it goes down?

## Health check

The `check=` for this node (in `.ibis/GRAPH.dot`):

```sh
{{NODE_CHECK}}
```

Exit 0 means healthy. The check runs every 2 minutes when `poll="fast"` is set.

## When it fails

TODO: the documented fix procedure. Read this BEFORE guessing.

1. Confirm the failure: run the `check=` by hand.
2. ...
3. Verify the fix: re-run the `check=` and `tests/ibis/{{NODE_ID}}.sh`.

## Test

`tests/ibis/{{NODE_ID}}.sh` asserts this node's contract.

## Audit assertions

> `ibis audit` runs each fenced `ibis-assert` block below and requires exit 0 —
> this is how the doc *proves* it is still true. Replace the placeholder with a
> real claim this doc makes (an endpoint's shape, a file's existence, a version).

```ibis-assert
true   # TODO: assert something this doc claims is currently true
```

<!-- ibis-stamp: unset -->

